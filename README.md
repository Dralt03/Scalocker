# Scalocker

<br>

Hey there! In this project I've tried to build a project that simulates Docker's CLI/daemon architecture, in Scala, with lots of fun stuff like our own language for builds / programs, caching of intermediate build steps, potentially a build registry
